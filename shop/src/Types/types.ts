export interface ICars {
  name: string;
  price: number;
  acceleration: number;
  urlPhoto: string;
  description: string;
  fuel: string;
  details: string;
}

export interface ITypes {
  name: string;
  img: string;
}
